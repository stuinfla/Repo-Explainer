# Tooling noise
node_modules/
.vercel/
.DS_Store
*.log

# Secrets never go in git
.env
.env.*

# NOTE (recipe v1.3.1): the drop-in zip in downloads/ IS committed so Vercel serves it
# directly behind the .dropzone download button. downloads/ruqu-dropin.zip must NOT be ignored.
.vercel
